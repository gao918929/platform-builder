# 平台建造器 (Platform Builder) 操作说明

## 一、起步

### 合成

| 物品 | 配方 |
|------|------|
| **平台建造机** | 8×铁锭 围住 活塞（中）+ 红石块（底中） |
| **猪猪之证** | 钓鱼竿 + 金胡萝卜（无序合成） |

放置平台建造机后右键打开界面。

---

## 二、界面总览

界面分两种模式，左上角按钮切换：

- **设计模式**：网格画板 + 工具栏 + 材料面板 + 底部控制栏
- **放入模式**：机器库存 + 玩家物品栏，管理材料

---

## 三、设计模式

### 网格操作

| 操作 | 方式 |
|------|------|
| 缩放 | 鼠标滚轮（无级缩放 0.2× ~ 5×） |
| 平移 | 鼠标中键拖拽 |
| 撤销 | `Ctrl + Z`（最多 50 步） |
| 区块线 | 按 `G` 切换显示 |

### 六种工具

| 工具 | 快捷键 | 功能 |
|------|--------|------|
| **笔** | `B` | 点击/拖拽在网格上放置方块 |
| **擦** | `E` | 点击/拖拽移除网格上方块 |
| **框** | `R` | 拖拽绘制矩形 |
| **圆** | `C` | 拖拽绘制圆形（中点圆算法） |
| **线** | `L` | 拖拽绘制直线（Bresenham 算法） |
| **取色** | `P` | 点击已有设计吸取材料 |

### 材料系统

- 材料面板在网格下方，显示当前可用方块
- **点击材料**：选中；**再次点击**：取消选中
- 可用材料来源：机器库存中的方块、设计中的方块、猪猪之证解锁的混凝土

### 框选工具 (R) 特殊用法

- **选了材料 + 框选** → 释放后填充矩形
- **没选材料 + 框选** → 仅保留选区高亮，配合下方按钮使用

---

## 四、底部控制栏

```
速度: - +   Y: - 0 +   [扫描] [复制] [粘贴] [生成] [AI] [开始建造]
```

| 按钮 | 功能 |
|------|------|
| `速度 -/+` | 调整建造速度（1~500 块/tick） |
| `Y -/0/+` | 调整建造 Y 轴偏移（-64 ~ +64），控制机器上方/下方建造 |
| `[扫描]` | 扫描世界中方块（当前 Y 偏移层），以半透明色块显示 |
| `[复制]` | 将框选区域的方块（设计+已有）存入剪贴板 |
| `[粘贴]` | 将剪贴板内容贴到鼠标悬停位置（有半透明预览） |
| `[生成]` | 循环切换预设图案并生成（棋盘格→横条纹→竖条纹→边框→随机） |
| `[AI]` | 调用 AI API 自动生成地板设计（需配置） |
| `[开始建造]` | 将设计蓝图建造到世界中 |

### 扫描 & 复制粘贴流程

1. 调好 Y 偏移 → 点 `[扫描]` 看世界方块
2. 按 `R`，不选材料，拖出矩形选区
3. 点 `[复制]` 存入剪贴板
4. 移动鼠标到目标位置（看到半透明预览）
5. 点击网格设锚点 → 点 `[粘贴]`

### 图案生成流程

1. 在材料面板选一个材料
2. 按 `R` 框出生成区域（不框默认原点±5）
3. 点 `[生成]`，每次点击切一种图案
4. 不满意按 `Ctrl+Z` 撤销

---

## 五、放入模式（材料管理）

| 按钮 | 功能 |
|------|------|
| `[→ 放入全部]` | 将玩家物品栏中所有方块一键放入机器库存 |
| `[← 取出全部]` | 将机器库存中所有方块一键取出 |
| `[开始建造]` | 执行建造 |

支持拖拽管理材料。机器也会从相邻容器（箱子、漏斗等）自动取材料建造。

---

## 六、猪猪之证

放入机器任意库存槽位后：

- **16 色混凝土 + 16 色混凝土粉末**全部无限供应
- 设计模式的材料面板自动列出所有混凝土变体
- 建造时不消耗实际材料

---

## 七、AI 生成配置

在 `.minecraft/config/` 下创建 `platformbuilder.json`：

```json
{
  "api_url": "https://api.openai.com",
  "api_key": "sk-你的API密钥",
  "model": "gpt-4o-mini"
}
```

支持所有 OpenAI 兼容 API（硅基流动、DeepSeek、豆包等）。如需用国内服务，改 `api_url` 即可：

| 服务商 | api_url |
|--------|---------|
| OpenAI | `https://api.openai.com` |
| 硅基流动 | `https://api.siliconflow.cn` |
| DeepSeek | `https://api.deepseek.com` |

配置好后：
1. 调好 Y 偏移，点 `[扫描]` 看场地
2. 按 `R` 框出要生成的区域
3. 点 `[AI]`，等待几秒
4. AI 返回的布局自动填入设计网格

---

## 八、快捷键速查

| 快捷键 | 功能 |
|--------|------|
| `B` | 笔工具 |
| `E` | 橡皮擦 |
| `R` | 矩形工具 |
| `C` | 圆形工具 |
| `L` | 直线工具 |
| `P` | 取色工具 |
| `G` | 区块线切换 |
| `Ctrl + Z` | 撤销 |
